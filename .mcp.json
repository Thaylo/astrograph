{
  "mcpServers": {
    "astograph": {
      "command": "docker",
      "args": [
        "run", "--rm", "-i", "--pull", "always",
        "-v", ".:/workspace:ro",
        "--tmpfs", "/workspace/.metadata_astograph",
        "thaylo/astograph"
      ]
    }
  }
}
